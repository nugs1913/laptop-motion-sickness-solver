dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.1.0            # 센서 데이터 읽기
  flutter_background_service: ^5.0.0 # 백그라운드 실행 핵심
  flutter_local_notifications: ^18.0.0 # 백그라운드 알림 표시
  shared_preferences: ^2.3.0      # IP 주소 저장용